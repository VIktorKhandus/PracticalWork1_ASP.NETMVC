@model IEnumerable<FoodStore.Models.Manufacturers>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Производители</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Manufacturers" asp-action="Create" class="btn btn-primary">
                <i class="fas fa-plus"></i> &nbsp; Создать
            </a>
        </div>
    </div>

    <br /><br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Название
                    </th>
                    <th>
                        Описание
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var manufacturers in Model)
                {
                    <tr>
                        <td width="50%">@manufacturers.Title</td>
@*                         <td width="30%">@category.Description</td> *@
                        <td class="text-center">
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Manufacturers" asp-route-Id="@manufacturers.Id" asp-action="Edit" class="btn btn-primary mx-2">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <a asp-controller="Manufacturers" asp-route-Id="@manufacturers.Id" asp-action="Delete" class="btn btn-danger mx-2">
                                    <i class="far fa-trash-alt"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p> Нет действующих категорий </p>
    }

</div>
